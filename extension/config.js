// Same Firebase project as the web app (frontend/.env).
// apiKey is safe to expose in client code — Firestore security rules do the
// actual access control, not this key.
export const FIREBASE_CONFIG = {
  apiKey: 'AIzaSyCMNgntEubRWOFNkeTdL9wQVSibxmj-JA8',
  projectId: 'digital-wellness-4eb23',
}
